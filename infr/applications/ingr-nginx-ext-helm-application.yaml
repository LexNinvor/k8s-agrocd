apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ingress-nginx-external-helm
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default
  
  sources:
  - repoURL: 'oci://harbor.lan/library/ingress-nginx'
    chart: ingress-nginx
    targetRevision: 4.14.0
    helm:
      valueFiles:
      - $values/infr/helm/ingr-nginx-ext-values.yaml

  - repoURL: 'https://github.com/LexNinvor/k8s-agrocd.git'
    targetRevision: master
    ref: values
  
  destination:
    server: https://kubernetes.default.svc
    namespace: ingress-nginx-external
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true